console.log("union is typescript");
var myInformation = ["dheeraj", 26];
// so this is the union data types means we are using both the data types like mixed.
var myDescription = function (myName) {
    console.log("my name is" + " " + myName);
};
myDescription("dheeraj upadhyay");
